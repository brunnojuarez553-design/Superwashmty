# Copiá este archivo como referencia. Las variables reales se cargan en Vercel
# (Project Settings → Environment Variables), NO subas un .env con la key real a GitHub.

GROQ_API_KEY=tu_api_key_de_groq
# Opcional: si Groq falla, el asistente cae a OpenAI automáticamente.
OPENAI_API_KEY=
